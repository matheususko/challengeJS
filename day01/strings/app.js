// métodos comuns de string
const email = 'matheususko@gmail.com'

// ele vai obter o index da ultima ocorrencia do caracter que passarmos por argumento.
/* 
const lastIndexOfA = email.lastIndexOf('t')
console.log(lastIndexOfA) */

// método slice (fatiar)
/* 
const emailSlice = email.slice(6, 11)
console.log(emailSlice, email)
*/

// método replace troca uma string por outra

const emailReplace = email.replace('a', '-')
console.log(emailReplace, email)